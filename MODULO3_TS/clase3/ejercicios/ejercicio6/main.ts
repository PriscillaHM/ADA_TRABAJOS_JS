import * as geometry from './geometry'

const circleArea = geometry.Geometry.areaOfCircle(5)
const squereArea = geometry.Geometry.areaOfSquare(4)

console.log('Area del circulo: ', circleArea)
console.log('Area del cuadrado: ', squereArea)