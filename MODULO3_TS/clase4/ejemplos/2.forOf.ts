//Ejemplo de for of en array
//En este claso el bucle for of recorre los valores del array
export let leguajes = ['JS', 'Python', 'TS']
for(let lenguaje of leguajes){
    console.log(`Ejemplo array for of: ${lenguaje}`)
}

//Ejemplo for of en string 
let saludo = 'Hola'
for(let letra of saludo){
    console.log(`Ejemolo de for if en string: ${letra}`)
}